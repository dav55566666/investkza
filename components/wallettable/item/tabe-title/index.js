import Dropdown from 'antd/lib/dropdown';
import React from 'react';
import { Menu } from "antd";
import WallettableTitletd  from '/components/tokens-tab/item/tabe-title/tabtitletd';
const WallettableTitle = ({menu2titp,children}) => {
    return (
      <tr className="tantitletr">
        {children}
      </tr>
    )
}

export default WallettableTitle;
