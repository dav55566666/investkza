import Dropdown from 'antd/lib/dropdown';
import React from 'react';
import { Menu } from "antd";
import RinoktableTitletd  from '/components/tokens-tab/item/tabe-title/tabtitletd';
const RinoktableTitle = ({menu2titp,children}) => {
    return (
      <tr className="tantitletr">
        {children}
      </tr>
    )
}

export default RinoktableTitle;
